<form method="post" action="{{ path('result_comment_delete', {'id': result_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ result_comment.id) }}">
    <button class="btn">Delete</button>
</form>
